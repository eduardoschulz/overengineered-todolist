info:
  name: Create Task
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/tasks/
  headers:
    - key: Content-Type
      value: application/json
    - key: Authorization
      value: Bearer PASTE_TOKEN
  body: '{"title":"Review PR #42","description":"Check auth middleware","status":"in_progress","assigned_to":"dev@test.com"}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
