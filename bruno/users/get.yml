info:
  name: Get User
  type: http
  seq: 2

vars:
  token: ''
  user_id: ''

http:
  method: GET
  url: http://localhost:8000/users/{{user_id}}
  headers:
    - key: Authorization
      value: Bearer {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
