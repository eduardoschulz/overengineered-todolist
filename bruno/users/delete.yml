info:
  name: Delete User
  type: http
  seq: 4

vars:
  token: ''
  user_id: ''

http:
  method: DELETE
  url: http://localhost:8000/users/{{user_id}}
  headers:
    - key: Authorization
      value: Bearer {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
