info:
  name: Delete User
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8000/users/PASTE_USER_ID
  headers:
    - key: Authorization
      value: Bearer PASTE_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
